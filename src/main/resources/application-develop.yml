spring:
  config:
    activate:
      on-profile: develop
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${{env.DB_URL}}
    username: root
    password: ${{env.DB_PASSWORD}}

  jpa:
    hibernate:
      ddl-auto: update